Description: Use a format specifier with gtk_message_dialog_new
Author: Stephen Kitt <skitt@debian.org>

--- a/source/unix/gtkui.cpp
+++ b/source/unix/gtkui.cpp
@@ -1348,7 +1348,7 @@
 }
 
 void create_messagewindow(char* message) {    
-	messagewindow = gtk_message_dialog_new(GTK_WINDOW(mainwindow), GTK_DIALOG_DESTROY_WITH_PARENT, GTK_MESSAGE_INFO, GTK_BUTTONS_OK, message);
+	messagewindow = gtk_message_dialog_new(GTK_WINDOW(mainwindow), GTK_DIALOG_DESTROY_WITH_PARENT, GTK_MESSAGE_INFO, GTK_BUTTONS_OK, "%s", message);
 	gtk_dialog_run (GTK_DIALOG (messagewindow));
 	gtk_widget_destroy (messagewindow);
 }
